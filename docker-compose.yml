services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: tender_hunter
      POSTGRES_USER: tender_hunter
      POSTGRES_PASSWORD: tender_hunter
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
